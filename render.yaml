services:
  - type: web
    name: crypto-miner-bot-web
    runtime: docker
    rootDir: .
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: TELEGRAM_TOKEN
        sync: false
