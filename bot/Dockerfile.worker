# Dockerfile.worker

FROM python:3.11-slim

WORKDIR /app

# Copy all files including bot and requirements
COPY . .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Command to start the Telegram bot
CMD ["python", "bot/bot.py"]
